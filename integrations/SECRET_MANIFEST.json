{
  "version": 1,
  "purpose": "Name-only secret inventory. Values must never be committed or printed.",
  "runtime_boundaries": {
    "supabase_edge_functions": [
      "OPENAI_API_KEY",
      "MATON_API_KEY",
      "MAKE_API_TOKEN",
      "MAKE_WEBHOOK_SIGNING_SECRET"
    ],
    "raspberry_pi_openclaw": [
      "MATON_API_KEY",
      "OPENROUTER_API_KEY",
      "PHONE_SSH_HOST_KEY_SHA256"
    ],
    "n8n_credentials_store": [
      "OpenClaw Queue Postgres",
      "OpenClaw Webhook HMAC"
    ]
  },
  "important": [
    "A Supabase Edge Function secret is visible only to Edge Functions; it is not automatically available to Raspberry Pi OpenClaw.",
    "Use one runtime-local SecretRef or a narrowly scoped authenticated proxy instead of copying values into GitHub or Telegram.",
    "Presence checks may report only present/missing, never hashes, prefixes or values."
  ],
  "secret_values_embedded": false
}
